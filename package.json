{
  "name": "bpsf-client",
  "version": "1.0.0",
  "private": true,
  "description": "Buy Per Square Foot — static frontend (HTML/CSS/JS, no build step)",
  "scripts": {
    "dev": "npx --yes serve . -l 5173 --no-clipboard",
    "start": "npx --yes serve . -l 5173 --no-clipboard"
  }
}
